#####################
# Application keybindings #
#####################

# Launch Browser (Firefox-esr)
super + b
    librewolf
    
# Launch Private Browser (Firefox Incognito Mode)
super + shift + b
	librewolf -private-window
    
# Launch Rofi Application Menu
super + space
   	rofi -show drun -modi drun -line-padding 4 -hide-scrollbar -show-icons -theme ~/.config/rofi/config.rasi
   	 
# help with shortcuts
super + slash
	~/.config/scripts/help
	
# Reload sxhkd (Hotkey Daemon)
super + Escape
	pkill -USR1 -x sxhkd; notify-send 'sxhkd' 'Reloaded config'
    
# DWM Layout Menu
super + shift + l
    ~/.config/scripts/dwm-layout-menu.sh

# Attach focused window to tab group
super + t
    ~/.config/scripts/dwm-tabs attach

# Detach focused window from tab group
super + alt + t
    ~/.config/scripts/dwm-tabs detach

# Launch File Manager (Thunar)
super + e
	thunar

# Launch Geany (Text Editor)
super + f
	geany

# Launch GIMP (Image Editor)
super + g
	gimp
	 
# Helium browser
super + c
    helium
    
# Helium browser (incognito)
super + shift + c
    helium --incognito

# Launch Discord	
super + d
	Discord

# Launch OBS (Screen Recording/Streaming)
super + o
	obs
	
# Increase Volume (Custom Script)
super + Insert
    ~/.config/scripts/changevolume up

# Decrease Volume (Custom Script)   
super + Delete
	~/.config/scripts/changevolume down
	
# Toggle Mute (Custom Script)
super + m
	~/.config/scripts/changevolume mute
	
# Dedicated Key for Volume Up
XF86AudioRaiseVolume
	~/.config/scripts/changevolume up
	
# Dedicated Key for Volume Down
XF86AudioLowerVolume
	~/.config/scripts/changevolume down
	
# Dedicated Key for Mute
XF86AudioMute
	~/.config/scripts/changevolume mute

# Increase Screen Brightness
XF86MonBrightnessUp
	xbacklight +10

# Decrease Screen Brightness
XF86MonBrightnessDown
	xbacklight -10
	
# Take a Screenshot (Region Select)
super + Print
	flameshot gui --path ~/Screenshots/

# Take a Fullscreen Screenshot
Print
	flameshot full --path ~/Screenshots/
	
# Take a Screenshot (Region Select)
super + shift + s
	flameshot gui --path ~/Screenshots/

# Take a Fullscreen Screenshot
super + s
	flameshot full --path ~/Screenshots/
    
# Power Off/Reboot
super + x 
	~/.config/scripts/power
